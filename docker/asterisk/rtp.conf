; RTP configuration for mdsiprtp integration testing

[general]
; RTP port range
rtpstart=10000
rtpend=20000

; DTMF mode
dtmfmode=rfc2833

; Enable RTCP
rtcpinterval=5000

; ICE support (disabled for basic testing)
icesupport=no

; STUN server (optional)
; stunaddr=

; NAT settings
nat=no
