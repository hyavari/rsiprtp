# Single-crate collapse — implementation journal

**Started:** 2026-05-01 (overnight, Arthur asleep)
**Plan:** `wrk_docs/2026.05.01 - HLD - Collapse workspace into single rsiprtp crate.md`
**Supervisor:** Claude (this agent — orchestration only, not editing code directly)

## Team model

- **Implementer** — does the code work, one stage at a time.
- **Reviewer** — runs the `code-reviewer` skill against the implementer's output.
- **Apply-feedback** — runs the `code-review-apply-feedback` skill on review findings.
- **Devil's advocate** — challenges the work after each stage, looks for what's been swept under the rug, asks "what would break this?"

Supervisor (me) reads agent summaries, decides what's good enough to ship vs. needs another pass, commits between stages, and keeps this journal.

## Stages

| # | Stage | Status |
|---|---|---|
| 1 | Mechanical collapse (HLD steps 1–14) | DONE |
| 2 | Item 15: clippy `collapsible-match` (3 errors) | DONE — turned into 4 MSRV/clippy fixes |
| 3 | Items 16–17: Windows + macOS flaky tests | DONE — both were real production bugs, not flakes |
| 4 | Item 18: MSRV `--locked` build | pending |
| 5 | Item 21: CHANGELOG.md | pending |
| 6 | Item 22: version field verification | pending |
| 7 | Item 20: visibility tightening (open-ended) | pending |
| 8 | Item 19: doc audit + restore `deny(missing_docs)` (open-ended) | pending |
| 9 | Regenerate public-api.txt + final validation matrix | pending |
| 10 | Plan-vs-impl verification by independent agent team | pending |

Order: bounded items 15–18, 21, 22 first to lock in a green tree, then the open-ended items 20, 19 against the smaller surface, then final validation.

## Decisions / deviations

**Stage 1 closed 2026-05-01:**
- **`dtls` feature dropped entirely.** Devil's advocate found it was empty (no `#[cfg(feature = "dtls")]` gates anywhere); investigation confirmed via `git show 0e9478b:crates/rsiprtp-srtp/src/dtls.rs` that the original DTLS module never directly used openssl — it was always scaffolding. Cleaner to drop the misleading feature + optional openssl dep than to invent gating just to justify keeping it. README + lib.rs claims toned down to "SDES key exchange; DTLS framing types present but handshake not yet implemented." Honest about the state.
- **Dropped `crossbeam` and `dasp` deps.** Reviewer flagged as unused; verified and removed.
- **Empty `crates/rsiprtp-transport/` directory remains** on disk due to Windows file lock. Untracked, not in git index, not in cargo workspace — purely cosmetic noise. Will resolve at next reboot.
- **Fuzz `dtls` feature** dropped from `fuzz/Cargo.toml` (consequence of removing the feature itself).
- **Hand-written 10-message SIP fuzz seed corpus** lost in the move; was never in git anyway. Acceptable; reconstructable.

**Validation at Stage 1 close:**
- `cargo build --workspace --exclude gabby` — clean
- `cargo test --workspace --exclude gabby -- --test-threads=1` — 2592 lib tests + integration all green
- `cargo doc --no-deps` and `--all-features` — clean
- `cargo deny check` — clean
- `cargo package --list -p rsiprtp` — 94 files, 4.3 MiB / 482 KiB compressed; baresip/asterisk fixtures correctly excluded.

## Risk register

- **Scope of doc audit unknown until merged tree compiles and `cargo doc` runs.** Plan tolerates this.
- **Visibility tightening can break gabby/fuzz silently if I rely only on `cargo build -p rsiprtp`.** Build all three after each batch.
- **MSRV fix may require dep version pinning, which interacts with cargo-deny multi-version rules.** Deal with that interaction inside the same commit.

## Log

- 2026-05-01 22:30 — journal created, kicking off Stage 1.
