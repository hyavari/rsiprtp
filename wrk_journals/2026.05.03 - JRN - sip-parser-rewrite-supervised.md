---
title: SIP parser rewrite — supervised implementation
date: 2026-05-03
supervisor: Claude (opus-4-7)
worktree: parser-rewrite-exploration
hld: wrk_docs/2026.05.03 - HLD - sip-parser-rewrite.md
---

# Goal

Implement the 12-milestone parser rewrite from the HLD using an agent team.
Supervisor (this file's author) does **no implementation, testing, or
debugging directly** — purely orchestration, review-of-review, and
direction. Aim: 100% feature complete, fully diff-tested against rsip,
ready for a 0.4.0 release.

# Agent team

Each milestone runs through this pipeline:

1. **Implementation agent** (general-purpose) — writes code per the
   milestone scope. TDD where appropriate (tests first, then impl). Runs
   touched-path tests to verify before returning.
2. **Code reviewer** (using `code-reviewer` skill) — reviews the diff for
   quality, correctness, conventions.
3. **Apply-feedback agent** (using `code-review-apply-feedback` skill) —
   acts on the review's REQUIRED items and any SUGGESTED ones in scope.
4. **Devils advocate** (general-purpose) — challenges decisions made by
   the impl + review + apply-feedback chain. Reviews the supervisor's
   stage-gate decision too. Returns either "endorse" or specific concerns
   to address before commit.
5. **Supervisor** (me) — verifies, commits the milestone atomically,
   updates this journal, advances.

# Anti-patterns to avoid (from prior overnight pipelines)

- **Sub-agent zombie wait.** Sub-agents that kick off long processes
  (full_test, fuzz) and exit expecting system notification return
  prematurely. Either wrap waits in supervisor-side `Bash run_in_background`
  / `Monitor`, or instruct the sub-agent that the wait IS its job. Prefer
  the former.
- **`[N/7] PASS` grammar.** full_test driver doesn't print explicit
  `[7/7] PASS`. Detect completion via the dated HTML report file landing
  in `crates/rsiprtp/tests/results/` or via the `cargo run` exit code.
- **Stale git-status snapshots.** Multi-agent activity may have advanced
  HEAD between the system context's snapshot and the supervisor's first
  tool call. Refresh `git log` first thing.

# Working directory

All agents operate against the **worktree** at
`d:\language\rsiprtp-parser\` on branch `parser-rewrite-exploration`.
Every dispatch must specify this path; agents inherit cwd
`d:\language\rsiprtp\` otherwise.

# Cadence

Sequential by milestone. Each milestone:
- Dispatch impl agent → wait → on return, dispatch reviewer
- Dispatch reviewer → wait → on return, dispatch apply-feedback
- Dispatch apply-feedback → wait → on return, dispatch devils-advocate
- Dispatch devils-advocate → wait → on return, supervisor verifies + commits
- Update journal, advance

Optional shortcut for trivial milestones: skip apply-feedback if reviewer
returns "no REQUIRED items"; let devils-advocate confirm.

# Log

(entries appended below)

## 2026-05-03 — kickoff

- HLD committed at `a9f2366` on `parser-rewrite-exploration`.
- Worktree clean except this journal (untracked).
- 12 milestones planned per HLD §Phasing. Final verification phase after.
- Beginning milestone 1.

## M1 — parser/ skeleton + Method + StatusCode ✅

**Impl agent** (`a614971feaa8fa2db`): created `crates/rsiprtp/src/sip/parser/{mod,method,status}.rs`, modified `sip/mod.rs` to declare `pub(crate) mod parser`. `Method` has 14 variants (+`Publish`, `Prack` lowercase second char), case-insensitive `FromStr`, uppercase `Display`, predicates `creates_dialog` / `requires_ack`. `StatusCode` is a faithful port of mdsiprtp3 with one added boundary test. 10 tests, all four verification gates green (`cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test sip::parser::`, `cargo build`). Acknowledged TDD-shortcut up front: tests + impl in one pass because tests referencing nonexistent types fail-as-compile-error not fail-as-test, which is meaningless RED.

**Reviewer** (`ab6eb02438fa8fc5c`, code-reviewer skill): "Looks good — approve." No REQUIRED. One OPTIONAL: round-trip insurance via auto-asserted ALL slice. Endorsed all three impl-agent deviations. Confirmed `StatusCode` is byte-faithful to mdsiprtp3.

**Apply-feedback agent**: skipped (no REQUIRED items). Per journal protocol.

**Devils advocate** (`acd3c078204cb5a35`): "ENDORSE with two NITs." Independently verified the TDD-defensibility claim by reading verbatim mdsiprtp3/src/sip/method.rs:75-92 and confirming the new-behavior tests (CI parse, `Publish` variant) would genuinely fail there. Ran full `cargo test -p rsiprtp` (not just parser::) — full suite green. Two NITs to carry as M2 backlog: (a) the ALL-slice auto-round-trip; (b) `is_final()` doc-comment polish ("≥200" not "is in known final-class range").

**Supervisor verdict**: advance to M2. Two NITs noted in M2 backlog below.

### M2 backlog (carried from M1)

- `crates/rsiprtp/src/sip/parser/method.rs` — add a test that asserts every variant in a `const ALL: &[Method]` slice round-trips through `as_str() → FromStr → variant` (5 lines, catches drift between `as_str()` arms and `FromStr` table).
- `crates/rsiprtp/src/sip/parser/status.rs:76` — clarify `is_final()` doc to "any code ≥200" rather than implying "in a known final-class range" (out-of-spec codes like u16::MAX still report `is_final() == true`).

## M2-A — Header + Headers + framing + Request/Response ✅ (commit `f6f5b5b`)

**Impl agent** (`aa66395663e705fb4`): created `parser/{header,framing,message}.rs`. 19 typed `Header` variants + `Other`, `Vec<Header>`-backed `Headers` (no HashMap — wire round-trip needs order), `split_message` / `parse_header_block` with folding / `parse_request_line` / `parse_status_line`, `Request`/`Response`/`Message` types with `parse` and `to_bytes`. 60 tests. All gates green. Caught HLD inconsistency: HLD said "21 typed variants" but actual usage is 19 + Other. Implemented 19 (correct count).

**Reviewer** (`aae24df69b3b04d0b`, code-reviewer skill): "Looks good — minor changes." No REQUIRED. Several SUGGESTED including: `o → Event` compact form missing (RFC 3265), dead defense-in-depth in framing.rs MAX_HEADERS recheck, misleading `itoa_u16` helper name, untested lax behaviors should be pinned. Confirmed all 3 impl deviations.

**Apply-feedback** (`a1a745bf553ef083b`, code-review-apply-feedback skill): applied 5 SUGGESTED items. Added `o`/`u`/`d` compact forms (RFC 3265 + RFC 3261 §20.11). Extended scope (with notification): compact-form letters whose long form isn't typed now resolve to `Header::Other(<long-form-name>, value)` rather than `Other(<letter>, value)` — correct canonical-wire behavior. Inlined `itoa_u16`. Added two pinning tests for lax behaviors. Fixed HLD count drift. 155 tests all green.

**Devils advocate** (`abf9ae3ccf379481f`): ENDORSE with one CONCERN and three NITs. CONCERN: `to_bytes()` emitted stale `Content-Length` when the parsed body had been truncated to less than the header's claimed length, yielding wire-corrupt re-serialization. NITs: HLD line 151 still said "26" not "28"; per-header fold-count cap missing; some adversarial test density gaps. Independently verified Content-Length round-trip behavior by tracing the code.

**Polish agent** (`ae0ae1ff9de026869`): applied DA's CONCERN (auto-rewrite Content-Length on serialize from `self.body.len()` if the header is present; do not add one if absent) + HLD count fix. Added 4 round-trip tests. **Hit zombie-wait anti-pattern**: returned with "Compile is in progress. I'll wait for the Monitor event" instead of waiting for verification commands. Supervisor verified directly via background bash: `cargo test -p rsiprtp --lib parser::` → **159 passed, 0 failed**. Code work was correct; agent just bailed on reporting.

### M2-A backlog (carry to M2-B / M11)

- Per-header fold-count cap in `framing.rs` (currently only total-length cap).
- Adversarial micro-tests: empty input, single-byte input, embedded NUL.
- Will be exercised by M2-B's diff harness and M11's fuzz campaign.

**Supervisor verdict**: M2-A green. Advance to M2-B (fixtures + diff harness).

### Lessons-learned: agent zombie-wait

Polish agent had explicit "the wait IS your job" in brief. Still bailed. Pattern: agents with a mix of write-tools and verification-tools will preferentially write and assume the system runs the verification. Mitigation for future dispatches: structure the brief so the LAST work item is the verification command output as a literal copy-paste in the return — gives the agent a tangible "I am not done until I have this string" goalpost.
