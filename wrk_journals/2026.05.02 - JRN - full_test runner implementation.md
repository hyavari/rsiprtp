# 2026.05.02 - JRN - full_test runner implementation

Implementing the design in
`wrk_docs/2026.05.02 - HLD - full_test runner - V2.md`.

Reference: `d:\language\mddosem\src\bin\full_test.rs` (the model).

## Plan

I'm orchestrating agents through these stages, committing after each:

1. **Stage 1 — Implementation.** One agent writes the scaffold + all
   passes + report generation + main(), with internal unit tests
   where they pull weight (parsers, path-collision, categorizer).
   Verifies `cargo build -p full_test` and `cargo run -p full_test
   --skip-coverage` runs end-to-end on this tree.
2. **Stage 2 — Code review.** code-reviewer agent reviews the diff
   against rust-best-practices and the V2 HLD. Then a fixer agent
   applies the actionable feedback (code-review-apply-feedback).
3. **Stage 3 — Devil's advocate.** Independent agent reads the V2
   HLD and the diff cold and challenges scope/correctness/edge
   cases. Fixer agent addresses anything material.
4. **Stage 4 — Ancillary deliverables.** README, CLAUDE.md,
   `.gitignore`, `.gitkeep` (anything Stage 1 didn't cover).
5. **Stage 5 — Completeness pass.** Compare implementation against
   V2 HLD line by line. Iterate fixes until 100% match.

## Ground rules I'm holding the agents to

- The runner is a dev tool — keep it small. No premature abstractions.
- Match the project's CI command idioms (`--workspace --exclude gabby
  --exclude full_test`, `RUSTDOCFLAGS=-D warnings`, `--test-threads=1`).
- `chrono` only as a third-party dep. HTML escape and JSON scrape
  inline (~30 LoC each, copy from mddosem).
- Every cargo invocation excludes `gabby` and `full_test` to dodge
  the Vosk dep and the Windows self-rebuild lock.
- Optional tools (`cargo-deny`, `cargo-audit`, `cargo-llvm-cov`)
  probed with `--version`; missing → warn + skip, do not fail.
- One HTML report per run, dated path with `(N)` collision suffix.
- Only `chrono`, no `tempfile`, no `colored`, no `base64` (no
  screenshots).

## Log

### 2026.05.02 — Kickoff

Plan above; spawning Stage 1 implementation agent next.

### 2026.05.02 — Stage 1 complete

Implementation agent delivered:

- `tools/full_test/Cargo.toml` (16 LoC, `chrono` only, `publish = false`).
- `tools/full_test/src/main.rs` (1625 LoC after `cargo fmt`).
- `crates/rsiprtp/tests/results/.gitkeep`.
- Workspace `Cargo.toml`: `tools/full_test` added to `members`,
  `default-members` unchanged.

8 unit tests added (parse_test_output, record_non_test_cargo_failure,
build_report_path, categorize_tests, parse_options, extract_json_number,
html/markdown smoke). `cargo test -p full_test` and
`cargo clippy -p full_test --all-targets -- -D warnings` both clean.

End-to-end smoke (`--skip-coverage --skip-supply --skip-quality`)
produced an HTML report at the documented path. Exit code 1, but the
two failures are environment artifacts (Windows linker file-lock on
the rsiprtp test binary + downstream `cargo doc` failure) — the
runner correctly synthesized a `cargo::test::process_failure` record
with stderr tail. The runner itself worked.

**LoC.** 1625 vs the spec's ~600 estimate. The bulk is the embedded
HTML/CSS literal and the report builders. Function decomposition
matches the spec; no abstraction was added. Leaving as-is.

**Deferred to Stage 4** (intentionally scoped out of Stage 1):
gitignore line, README "Testing" subsection, CLAUDE.md one-liner.

Committing Stage 1 now, then code review.

### 2026.05.02 — Stage 2 complete

Code review found nothing blocking. Six suggestions, all worth doing —
applied them in one pass:

1. **Coverage exit-code gap.** `CoverageResult` became an enum
   (`Skipped` / `Failed { stderr_tail }` / `Ok(metrics)`). A genuine
   `cargo llvm-cov` crash now flips overall exit code and renders a
   Coverage FAIL section. Previously a coverage crash was silently
   indistinguishable from "tool not installed".
2. **JSON scrape replaced with serde_json.** `extract_json_number`
   deleted; `parse_coverage_json` now uses `serde_json::Value`
   lookups against `data[0].totals.<section>.<key>`. `serde_json`
   added as a dep (workspace inheritance). Tests adapted, plus three
   new ones including the nested-subobject case that was the
   original concern.
3. **Quality-check duplication.** ~120 LoC of scaffolding collapsed
   into a single `run_quality_check(name, args, is_issue)` helper
   with a `skipped_quality` constructor. The four `run_*` quality
   functions are now 3–7 line wrappers.
4. **Dead `process_failure` field** removed.
5. **Redundant `tool_available("llvm-cov")`** call in `main` removed
   (kept the canonical site inside `run_coverage`).
6. **`run_doc` stderr retention** gated on `!passed`.

Net: +330 / −249, 13 unit tests (was 8), all clean under fmt + clippy
+ build. End-to-end smoke produced a report at the documented path
with the new Coverage FAIL section visible.

Committing Stage 2 now, then devil's advocate.

### 2026.05.02 — Stage 3 complete (devil's advocate + fix pass)

DA found three substantive issues the previous reviewer missed:

1. **`cargo audit` permanent false-FAIL.** `RUSTSEC-2023-0071` and
   `RUSTSEC-2025-0134` are explicitly ignored in `deny.toml`, but
   `cargo audit` doesn't honor that file. Running both was redundant
   with cargo-deny's advisories check AND would have made the runner
   permanently red. Drop `cargo audit`. cargo-deny is authoritative
   locally.
2. **No `cargo build --locked`.** CI runs it on every job; lockfile
   drift CI catches would pass locally. Real gap.
3. **`cargo deny` can't distinguish transient from real failure.**
   Network failure → "failed to fetch" / "unable to update" → should
   be SKIP (transient), not FAIL. Real policy violations still FAIL.

Plus: doc-tests bucket (was falling through to "Integration"),
tighter quality filters (`Diff in ` for fmt; line-start anchored
`warning: ` / `error:` / `error[` for clippy/deny).

V2 HLD amended in place to capture all of the above (revisions log
section added, cargo command table updated, scope-outs for timeouts /
SIGINT / report retention made explicit).

Fixer agent applied all five items. Net +296 / −45 in main.rs;
20 unit tests (was 13), all clean under fmt + clippy + build.

Two end-to-end smokes, both exposed REAL pre-existing issues
elsewhere in the tree (concurrent agent work):
- doc FAIL: pre-existing intra-doc link warning in
  `crates/rsiprtp/src/session/manager.rs:756`.
- fmt FAIL: 26 "Diff in" issues (concurrent work hasn't been
  formatted yet).
- deny FAIL: 25 advisory/license violations (pre-existing project
  state).

These are exactly the things the runner exists to surface — the
runner is working correctly. None are bugs in my work.

Committing Stage 3 now, then Stage 4 (ancillary deliverables).

### 2026.05.02 — Stage 4 complete

Trivial doc/config edits, no agent needed:

- `.gitignore`: added `crates/rsiprtp/tests/results/*.html`. The two
  HTML reports already in that dir are unstaged; `git add .` from this
  point won't pull them in.
- `README.md`: new "Testing" section between Companion and Contributing,
  pointing at `cargo run --release -p full_test`. Cross-links to the V2
  HLD.
- `CLAUDE.md`: one-line addition under Build Commands.

Committing Stage 4 now, then completeness pass.
