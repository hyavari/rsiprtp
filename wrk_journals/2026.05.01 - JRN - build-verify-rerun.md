# 2026-05-01 — Build verification re-run

## Goal
Re-verify the workspace compiles cleanly with no warnings or errors, that
`cargo fmt --check` is clean, and that `cargo clippy --workspace -- -D warnings`
is clean. Investigate root cause of any warnings; fix defects, do not silence.

## Starting state
- Branch: `main`, up to date with `origin/main`
- Working tree: `M README.md` only (pre-existing publishing-prep edit)
- Earlier today's `build-warning-sweep.md` reported all checks green after a
  one-time `cargo fmt` correction (commit `8ca1f0c`)
- Goal here: confirm nothing has drifted since that sweep

## Plan
1. `cargo fmt --check` (workspace)
2. `cargo build --workspace --exclude gabby --all-targets` — must be warning-free
3. `cargo clippy --workspace --exclude gabby --all-targets -- -D warnings`
4. If any new diagnostics appear: investigate root cause, fix, re-verify.
5. Commit any fixes; do not bundle the unrelated README.md edit.

## Findings

### `cargo fmt --check`
Exit 0. No diffs. The earlier sweep's `cargo fmt` (commit `8ca1f0c`) is still
the latest formatting pass; nothing has drifted.

### `cargo build --workspace --exclude gabby --all-targets`
```
   Compiling rsiprtp v0.2.0 (D:\language\rsiprtp\crates\rsiprtp)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 14.93s
```
No warnings. No errors. Single crate (the consolidated `rsiprtp` v0.2.0 from
the Stage 1 single-crate collapse) builds clean across lib + all integration
test targets + benches + examples.

### `cargo clippy --workspace --exclude gabby --all-targets -- -D warnings`
```
    Checking rsiprtp v0.2.0 (D:\language\rsiprtp\crates\rsiprtp)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 5.33s
```
Zero diagnostics. `-D warnings` is satisfied trivially because clippy emitted
nothing.

### Gabby
`VOSK_LIB_DIR` not set in this shell. Per project `CLAUDE.md`, gabby is excluded
from default-members because it requires the Vosk native library on Windows;
building it is gated on that environment variable. Nothing actionable here —
not a regression, just the documented out-of-band build requirement.

## Outcome
Workspace (excluding gabby, as documented) is clean across `fmt`, `build
--all-targets`, and `clippy --all-targets -- -D warnings`. No warnings to
investigate, no defects surfaced, no missing code. No code changes required;
no commit produced. The pre-existing `M README.md` in the working tree is
publishing-prep work owned by an unrelated change and is intentionally not
touched.

