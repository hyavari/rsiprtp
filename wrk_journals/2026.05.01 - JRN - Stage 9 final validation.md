# Stage 9 — Final validation matrix

**Date:** 2026-05-01
**Author:** Claude (Opus 4.7, 1M context) for Arthur
**Branch:** main
**Predecessors:** Stages 1–8 (collapse, lint fixes, MSRV, doc audit, etc.)
**Reference HLD:** `wrk_docs/2026.05.01 - HLD - Collapse workspace into single rsiprtp crate.md`, "Validation" section.

## Validation matrix — results

| # | Gate | Result | Notes |
|---|------|--------|-------|
| 1 | `cargo build --workspace --exclude gabby` | PASS | Finished cleanly; nothing to recompile from prior session. |
| 2 | (was `--features dtls`) | N/A | The `dtls` feature was dropped in Stage 1; see HLD audit notes. Gate no longer applicable. |
| 3 | `cargo test --workspace --exclude gabby -- --test-threads=1` | PASS | All test binaries green on Windows. Doctests: 4 passed, 7 ignored, 0 failed (24.5 s). No flakes (registration / tag uniqueness fixes from Stages 2–3 hold). |
| 4 | `cargo clippy --workspace --exclude gabby --all-targets -- -D warnings` | PASS | No warnings; clean cache hit on stable rustc. |
| 5 | `cargo +1.88 build --workspace --exclude gabby --locked` | PASS | Compiles cleanly under MSRV 1.88 with the committed `Cargo.lock`. |
| 6 | `cargo doc -p rsiprtp --no-deps --all-features` | PASS | Re-ran from a wiped `target/doc/`. Zero warnings. `#![deny(missing_docs)]` is active in `crates/rsiprtp/src/lib.rs`. |
| 7 | `cargo deny check` | PASS | "advisories ok, bans ok, licenses ok, sources ok". |
| 8 | `cargo check -p gabby` | PASS | The four `warning: gabby@0.1.0: ...` lines are informational `cargo:warning=` notices from the Vosk build script (no `VOSK_LIB_DIR` set, which is expected on this host); they are not compiler warnings and do not fail anything. |
| 9 | `cargo check --manifest-path fuzz/Cargo.toml` | PASS | Clean. |
| 10 | `cargo publish --dry-run -p rsiprtp` | **PASS** | Packaged 94 files / 3.7 MiB (448 KiB compressed). Verify build compiled in 8.67 s. Ended with `warning: aborting upload due to dry run`, which is the normal dry-run terminator. The two `warning: ignoring test ... is not included in the published package` lines are expected — they confirm `package.exclude` is dropping the baresip / asterisk integration tests as designed. |

## `cargo publish --dry-run` — details

Verbatim trailing output:

```
   Updating crates.io index
   Packaging rsiprtp v0.2.0 (D:\language\rsiprtp\crates\rsiprtp)
warning: ignoring test `asterisk_integration` as `tests\asterisk_integration.rs` is not included in the published package
warning: ignoring test `baresip_integration` as `tests\baresip_integration.rs` is not included in the published package
    Updating crates.io index
    Packaged 94 files, 3.7MiB (448.2KiB compressed)
   Verifying rsiprtp v0.2.0 (D:\language\rsiprtp\crates\rsiprtp)
   Compiling rsiprtp v0.2.0 (D:\language\rsiprtp\target\package\rsiprtp-0.2.0)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 8.67s
   Uploading rsiprtp v0.2.0 (D:\language\rsiprtp\crates\rsiprtp)
warning: aborting upload due to dry run
```

No remediation work required. The verify build (the one cargo runs against an
extracted tarball in `target/package/`) compiled cleanly with the slim
package — no test fixtures missing, no metadata gaps, no path-dep leaks.

## Consistency checks — final state

| Item | Expected | Actual | Result |
|------|----------|--------|--------|
| README MSRV line (`README.md:88`) | "Rust 1.88" | `MSRV: **Rust 1.88**.` | OK |
| README MSRV badge (`README.md:6`) | `1.88+` | `https://img.shields.io/badge/rustc-1.88+-orange.svg` | OK |
| CI workflow MSRV job (`.github/workflows/ci.yml:69-74`) | 1.88 | `name: MSRV (Rust 1.88)` and `uses: dtolnay/rust-toolchain@1.88.0` | OK |
| `[workspace.package].rust-version` | `"1.88"` | `rust-version = "1.88"` (`Cargo.toml:17`) | OK |
| `[workspace.package].version` | `"0.2.0"` | `version = "0.2.0"` (`Cargo.toml:15`) | OK |
| `CHANGELOG.md` `[0.2.0]` entry | present | `## [0.2.0] — 2026-05-01` (`CHANGELOG.md:20`) | OK |

## `public-api.txt` currency

Last regenerated in commit `3efa1e9` (Stage 7 — visibility tightening).
Only one source change has landed since: commit `0cd84dc` (Stage 8) flipped
`#![warn(missing_docs)]` to `#![deny(missing_docs)]` in
`crates/rsiprtp/src/lib.rs`. That is a lint level, not an API change.

To be certain, I regenerated with
`RUSTUP_TOOLCHAIN=nightly cargo public-api --manifest-path crates/rsiprtp/Cargo.toml --color never`
and `diff` against the checked-in file returned exit 0 (identical, both
18,925 lines). No commit needed.

## Outcome

All ten gates pass (gate 2 is N/A by design). Consistency checks pass.
`public-api.txt` is up-to-date. The crate is publish-ready. The work tree
is clean (no in-flight changes from this stage other than this journal).

The only remaining action before an actual `cargo publish` is the manual
crates.io upload itself — Stage 9 does not perform that.

## Risks / caveats for the supervisor

- **Linux/macOS coverage** — gate 3 was run on Windows only. CI handles
  the Linux/macOS legs; Stage 9 didn't trigger CI. If CI hasn't been
  green on the merge commit (whatever that ends up being), confirm it is
  before publishing.
- **`gabby` full build** — host has no `VOSK_LIB_DIR` so only `cargo check`
  was exercised. Per the prompt that's sufficient on this host. A full
  `cargo build -p gabby` will be needed on a Vosk-equipped host before any
  Gabby-targeted release.
- **Network dependency for `cargo deny check`** — the run hit the live
  advisory DB and was fine; offline runs may behave differently if the
  advisory cache is stale.

No blockers.
