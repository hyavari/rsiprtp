# 2026.04.29 - JRN - publishing-prep-autonomous

## Context

Autonomous run on the rsiprtp publishing-prep "autonomous bucket" agreed with Arthur. Today's plan: knock out the mechanical / low-judgment items so the rename + API surface review can land cleanly later.

**Constraints (self-imposed, based on Arthur's CLAUDE.md and conversation):**
- Don't push to GitHub. Local commits only — Arthur reviews before any push.
- Don't touch the other agent's in-flight work in `wrk_docs/` (two untracked files from 2026-04-29 about Opus/codec/congestion).
- Don't perform sweeping renames or refactors that could collide with concurrent work — those are out of scope for this session.
- Don't run another history scrub. We just did one.
- Skip anything from the "needs Arthur input" bucket: the `mdsiprtp` → `rsiprtp` rename, MSRV pin, fuzz triage, history scrubs of dev docs.

## Plan (this session)

1. License files at root: `LICENSE-MIT`, `LICENSE-APACHE`.
2. Workspace `Cargo.toml`: `license = "MIT"` → `"MIT OR Apache-2.0"`. Per-crate manifests inherit via `license.workspace = true`, so this cascades. Exception: `crates/gabby/Cargo.toml` carries an explicit `license = "MIT"` — change to dual too for consistency.
3. `crates/gabby/Cargo.toml`: add `publish = false`. Heavyweight Vosk/Ollama/Piper deps don't belong on crates.io.
4. Delete from HEAD (not history): `RALPH_PROMPT.md`, `GEMINI.md`, top-level `src/main.rs` (orphaned `cargo new` scaffolding — workspace `Cargo.toml` has no `[package]`, so this is dead weight). History scrub of these is deferred.
5. `.gitignore` audit — add anything obvious that's missing.
6. Community boilerplate: `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), `.github/PULL_REQUEST_TEMPLATE.md`, `.github/ISSUE_TEMPLATE/{bug_report,feature_request}.md`.
7. Clippy + fmt sweep. Run `cargo fmt --check` and `cargo clippy --workspace --all-targets -- -D warnings`. Fix anything that turns up. Last commit `1a363f1` was a clippy 1.94 sweep so should be mostly clean.
8. CI workflow at `.github/workflows/ci.yml` (Linux/macOS/Windows × stable + a fmt+clippy job + cargo-deny + cargo-audit). Existing `integration.yml` left alone.
9. Examples in `examples/` (workspace-level if possible, or under the facade crate). Minimal UAS + minimal UAC. Skip the call-recorder for now.

## Out of scope (deferred to future sessions, with Arthur)

- The rename: `mdsiprtp` → `rsiprtp` and the `pub use` re-export shuffle. Gates published metadata.
- Top-level Cargo.toml metadata bundle (description, keywords, categories, repository, homepage, docs.rs config) — depends on the rename.
- Per-crate metadata for crates that get published (under facade-only, ideally none — but if internal crates also need to be reachable on docs.rs, they'd need fields too).
- `cargo public-api` audit and what to expose via `pub use`.
- `#![warn(missing_docs)]` and crate-level `//!` rustdoc — cleaner to do post-rename.
- MSRV pin in `[package.rust-version]` and CI matrix.
- `cargo-fuzz` setup + harness for the SIP parser.
- Removing `wrk_journals/` / `wrk_docs/` from the public repo — those are useful internal context; can be split out later if desired.

## Progress log

(Updated as work proceeds. Saved frequently.)

### Items 5–6 done (.gitignore audit + community boilerplate)

- `.gitignore` expanded with the standard Rust/IDE/OS/coverage cruft: `*.rs.bk`, coverage outputs (`*.profraw`, `*.profdata`, `lcov.info`, `coverage/`), local-env files (`.env`, `.envrc`, with `!.env.example` carve-out), IDE configs (`.idea/`, `.vscode/`), editor temps (`*.swp`, `*.swo`, `*~`, `*.bak`), OS junk (`.DS_Store`, `Thumbs.db`, `desktop.ini`), and `*.log`. Kept the existing `target/`, gabby vendor/models, and `*-COMBINED.TXT` entries with their original comments.
- `SECURITY.md` — directs vuln reports to GitHub Security Advisories, names the in-scope crates, and sets a 7-day-ack / 30-day-update SLA. Calls out gabby explicitly as out of scope.
- `CONTRIBUTING.md` — quick-start build, code style (fmt + `-D warnings`), Sans-IO guidance, and a no-`unwrap()`-in-library-code rule that matches the existing codebase.
- `CODE_OF_CONDUCT.md` — slim file referencing the Contributor Covenant 2.1 by URL rather than embedding the full text. Initially tried to embed the full text and hit a content-filter API error, so dropped to the URL-reference pattern. Reporting contact set to Arthur's email.
- `.github/PULL_REQUEST_TEMPLATE.md` — Summary / Test plan / Notes-for-reviewer with a checklist mirroring the CI gates.
- `.github/ISSUE_TEMPLATE/bug_report.md` and `feature_request.md` — standard bug-report and feature-request fronts. Bug template explicitly asks for OS/network/Rust/version environment details since SIP bugs are usually environment-sensitive.

### Items 1–4 done (licensing + scaffolding cleanup)

- `LICENSE-MIT` and `LICENSE-APACHE` added at repo root with `2025-2026 Martin Davidson` copyright. Apache file is the unmodified ASF v2.0 boilerplate.
- Workspace `Cargo.toml`: `license = "MIT"` → `"MIT OR Apache-2.0"`. `authors = ["MD"]` → `["Martin Davidson <martingdavidson@gmail.com>"]`. All `mdsiprtp-*` crates inherit via `license.workspace = true`, so this cascades workspace-wide.
- `crates/gabby/Cargo.toml`: explicit `license = "MIT"` → `"MIT OR Apache-2.0"` to match. Added `publish = false` so gabby never accidentally hits crates.io (heavyweight Vosk/Ollama/Piper deps).
- Deleted `RALPH_PROMPT.md`, `GEMINI.md`, top-level `src/main.rs` (and the empty `src/` dir). The top-level `src/` was orphaned `cargo new` scaffolding — workspace `Cargo.toml` has no `[package]`, so nothing referenced it. HEAD-only delete; git history still has them (deferred).
- Going to commit this as one unit before moving on to .gitignore + community boilerplate.

### Snapshot

- Branch: `main` @ `e1735e4`, tracking `origin/main`, clean working tree before this session started.
- Untracked at start: `wrk_docs/2026.04.29 - Briefing - CongestionController to codec bitrate bridge.md`, `wrk_docs/2026.04.29 - HLD - Opus codec ropus 0.12 swap with typed runtime API.md` (other agent's, leave alone).
- 12 workspace members (11 mdsiprtp-* internal crates + gabby + the `mdsiprtp` facade).
- License: `"MIT"` (workspace + gabby).
- `.gitignore`: minimal (target/, gabby vendor + models, `*-COMBINED.TXT`).
- Existing CI: `.github/workflows/integration.yml` only.
- Top-level `src/main.rs`: `Hello, world!` scaffolding, orphaned (no `[package]` in workspace root).
