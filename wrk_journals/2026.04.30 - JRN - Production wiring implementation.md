# Journal — Production wiring for the bitrate bridge

**HLD**: `wrk_docs/2026.04.30 - HLD - Production wiring for the bitrate bridge.md`
**Sibling HLD (landed)**: `wrk_docs/2026.04.29 - HLD - CongestionController to codec bitrate bridge.md`
**Sibling commits**: `9c47f67`, `f5aa809`, `2c23898`, `8d3fc5c`

## Approach

Same supervisor pattern as the bridge work yesterday. Stages sequential because they layer:

1. **`RtcpCompound::parse`** in `rsiprtp-rtp` (foundational, independent).
2. **`SessionCodec` enum + Cargo feature** in `rsiprtp-session` (independent of Stage 1).
3. **`MediaSession` refactor** — depends on Stages 1 and 2; integration tests.
4. **Final HLD-vs-implementation audit** — iterate until 100 %.

Each stage: implement (TDD where it fits) → code-reviewer + devil's advocate in parallel → apply feedback → commit.

## Log

### 2026-04-30 — kickoff
- HLD locked after one self-review pass (4 fixes: REMB target inverted to 16 kbps for real drop; `encode_audio` becoming fallible called out; integration-test internal-state assertion dropped; Cargo.toml clarified to amend existing dep rather than promote dev-dep).
- Verified crate rename is complete: all package names and directories under `crates/` are `rsiprtp-*` (was `mdsiprtp-*` yesterday).
- Survey confirms the working tree is otherwise unchanged from yesterday's bridge close-out: no new RTCP plumbing, `MediaSession` still G711-only, `CongestionController` still uncalled in production. Greenfield API surface to add.
- Starting Stage 1.

### 2026-04-30 — Stage 1 complete (commit `909de3f`)
- Implementer added `RtcpCompound::parse`, `RtcpPacket::Unknown` variant, `SourceDescription::parse`, `Goodbye::parse`, plus 4 tests. The two added per-packet parsers were necessary surprise additions: the HLD assumed they existed (only `build()` did). Honest scope creep, sound symmetry.
- Reviewer + DA both said ship it. DA found one real bug: `Goodbye::parse` zero-length reason byte misreads padding `0x00` as `Some("")`. Also flagged two test-coverage gaps (walker-past-unknown-in-middle, AFB FMT 15 without REMB magic).
- **Action taken**: applied DA's BYE fix and added both tests. Total tests now 234 in `rsiprtp-rtp` (was 228; +6 over baseline = the 4 from the HLD plus these 2 safety tests).
- **Action declined**: DA's typed-error suggestion (`RtcpParseError` instead of `String`). Real but marginal — both error variants are equally "bad bytes" to the caller; not worth changing the HLD. The implementer's `format!("{e}")` boundary conversion is one line.
- **Working tree note**: another agent has 27 files staged for an unrelated workstream (Cargo versioning, README/LICENSE docs, etc.). Used `git commit --only -- crates/rsiprtp-rtp/src/rtcp.rs` to commit just my one file without disturbing their preparations.
- Starting Stage 2.

### 2026-04-30 — Stage 2 complete (commit `f5e0c44`)
- Implementer reported two surprises: (a) G.722 is **infallible** (HLD wrong on this — only Opus is fallible); (b) `OpusCodec` (~13 KB) and `G722Codec` (~944 B) trip clippy's `large_enum_variant` lint, so the variants are boxed. G.711 (1 byte) stays inline. `as_adaptive_mut` correctly returns `&mut dyn AdaptiveBitrate` through the Box.
- 7 new tests, 297 total; clippy / fmt / build clean.
- Reviewer + DA both said ship it. DA had 10 challenges; supervisor reviewed each:
  - DA #1 (drop G722 Box): clippy default 200B threshold, G722 at 944B still triggers it after Opus is boxed. Implementer's "both boxed" satisfies clippy without an `#[allow]`. Declined.
  - DA #4 (whitespace test): strict rejection assumed; existing unknown-encoding test catches accidental matches. Declined.
  - DA #5 (G722 samples_per_frame doc): real ambiguity but Stage 3 will decide consumption. Declined.
  - DA #8 (`#![warn(missing_docs)]` scope creep flag): clippy `-D warnings` is clean per implementer; if undocumented items existed, build would have failed. Audit will catch.
  - All others: polish or non-issues.
- **Action taken**: zero applies. Both reviewers say solid; DA's challenges are either declined or non-actionable. Committed Stage 2 directly.
- **Git note**: `git add` of my three files showed the staging area also contained the other agent's `transfer.rs`. Used `git commit --only -- file1 file2 file3` to commit only mine; their `transfer.rs` remained staged for their own commit.
- Starting Stage 3.

### 2026-04-30 — Stage 3 saga (commit `2c6f3da`)
- **First implementer hit rate limit** mid-task (~11 min runtime, 68 tool uses). Made substantial progress: call.rs +262, manager.rs +28, new integration test file. Did not commit.
- **Second agent (diagnosis)** verified the partial work was actually complete and correct — captured the byte-delta numbers (113→50, delta=63). Started to apply trivial fmt fixes.
- **Concurrent-agent collision**: while diagnosis was running, the publishing-prep agent reset / re-stashed the working tree. Outcome: 55 unrelated files appeared in the staging index; my call.rs and manager.rs reverted to HEAD; my untracked integration test file was deleted (untracked files aren't preserved by stash). Diagnosis agent correctly stopped rather than fight a concurrent worktree edit.
- **Recovery**: extracted call.rs and manager.rs from `stash@{0}` (which held both my Stage 3 work and the publishing-prep agent's bigger changeset), `git checkout stash@{0} -- file1 file2`. Build / clippy / 297 lib tests still passed.
- **Third agent** rewrote the integration test file from the HLD spec — same shape, same numbers (113→50, delta=63), no `set_remote(...)` needed since `encode_audio` doesn't require an active remote.
- Reviewer + DA in parallel against the recovered code: reviewer 100% green; DA had 15 challenges. Supervisor weighed each:
  - Applied DA #2: pull CC's initial bitrate from `OpusConfig::fullband_speech().bitrate` rather than the magic `32_000`, so they can't desync.
  - Applied DA #5: `tracing::warn!` when SDP-negotiated Opus parameters (clock_rate or channels) don't match the v1 fullband-mono build, so deployments notice the limitation.
  - Declined: tick-time-injection removal (Sans-IO consistency), CC bound tuning (HLD-anchored), tighten delta floor (already 3× margin), early-media error swallow (pre-existing pattern).
- **Commit oddity**: my Stage 3 commit only contained 2 files (call.rs + new test). manager.rs was missing because the publishing-prep agent's intervening commit `d6b20c1` had ALREADY included my manager.rs cascade fix in their changeset (presumably from their stash containing both). Net: all my work landed correctly, just split across two commits — not ideal for attribution but functionally clean.
- **Final state**: working tree clean, 4 commits ahead of origin/main, all tests passing. Starting final audit.

### 2026-04-30 — Final audit complete
- Auditor walked every HLD section against the three production-wiring commits plus the publishing-prep commit that swept up `manager.rs`.
- **Verdict**: COMPLETE WITH ACCEPTABLE DEVIATIONS.
- **Acceptable deviations** (all pre-disclosed during the work):
  1. `OpusCodec`/`G722Codec` boxed in `SessionCodec` to satisfy clippy's `large_enum_variant` lint.
  2. `SourceDescription::parse` and `Goodbye::parse` added in Stage 1 (HLD assumed they existed; only `build()` did).
  3. `manager.rs` cascade landed in the publishing-prep agent's commit `d6b20c1` rather than my Stage 3 commit (their stash had it mixed in). Attribution noted; functionally clean.
  4. Two supervisor-added safety tests for `RtcpCompound::parse` (`parse_walks_past_unknown_in_middle`, `psfb_fmt15_without_remb_magic_is_unknown`).
  5. Stage 3 hardenings applied via DA #2 + #5: CC initial sourced from `OpusConfig::fullband_speech()` (no desync); SDP-vs-Opus parameter mismatch logged at `warn`.
- **Gaps requiring iteration**: none.
- **Build/test outcomes**:
  - `cargo build --workspace --exclude gabby` — clean.
  - `cargo clippy -p rsiprtp-rtp --all-targets -- -D warnings` — clean.
  - `cargo clippy -p rsiprtp-session --all-targets -- -D warnings` — clean.
  - `cargo fmt --check` — clean.
  - `cargo test -p rsiprtp-rtp` — 234 passed.
  - `cargo test -p rsiprtp-session` — 297 lib + 1 bitrate_bridge_remb + 2 rtcp_drives_bridge passed.

## Summary

Three commits delivering the production-wiring API surface — the bridge can now actually do something when fed real RTCP traffic. Each stage went implement → reviewer + DA in parallel → apply small fixes → commit. The DA earned its keep at every stage:

- **Stage 1** — caught a real `Goodbye::parse` zero-length-reason bug (would have misread padding `0x00` as `Some("")` on non-self-built wire data) and two test-coverage gaps (walker-past-unknown-in-middle; AFB FMT 15 without REMB magic). All applied.
- **Stage 2** — surfaced 10 challenges, mostly polish; supervisor declined all (boxing decision was correct, `to_lowercase` per call is a non-issue, etc.).
- **Stage 3** — caught the magic-number CC initial / OpusConfig desync risk and the silent SDP-parameter ignoring. Both applied as defensive hardenings.

A multi-agent collision mid-Stage-3 deleted the integration test file and reverted lib code to HEAD; recovery via `git checkout stash@{0} -- file1 file2` for the lib code, fresh agent for the lost test file. The publishing-prep agent's commit `d6b20c1` swept up the `manager.rs` cascade fix from the same shared stash, so the file landed under their commit rather than mine — functionally clean, attribution noted.

Production status: with this HLD landed, the path "REMB bytes arrive on a UDP socket → `MediaSession::handle_rtcp` → `cc.on_remb` → `MediaSession::tick` → `BitrateBridge::poll` → Opus encoder rate change" is complete. The remaining gap is the UDP socket loop itself — a separately scoped piece of work (HLD § Non-goals).

NACK and RTT routing remain as documented v1 deferrals; the entry point in `MediaSession::handle_rtcp` is marked with the intent.
