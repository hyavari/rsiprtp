# Journal — Remove DTLS-SRTP stub

**Date:** 2026-05-02
**HLD:** `wrk_docs/2026.05.02 - HLD - Remove DTLS-SRTP stub.md`
**Brief:** `wrk_docs/2026.05.01 - Brief - DTLS-SRTP decision.md`
**Supervisor:** Claude (Opus 4.7, 1M context)

## Stage plan

1. **Code changes** — HLD steps 1, 2, 3, 6, 7. Delete `dtls.rs`, update `srtp/mod.rs` + `lib.rs`, drop fuzz target, annotate `security.rs`. Validate build/clippy/test. Commit.
2. **Doc changes** — HLD steps 4, 5. README + ARCHITECTURE. Commit.
3. **Release plumbing** — HLD steps 8, 9, 10. CHANGELOG promotion, workspace version 0.2.0 → 0.3.0, regenerate `public-api.txt`. Commit.
4. **Completeness audit** — agent compares implementation against HLD; iterate if gaps; final commit if needed.

Per stage: implement → code-reviewer → apply-feedback (if needed) → devil's-advocate → commit.

TDD note: this is pure deletion. No new behaviour to test-first. The pre-existing `cargo test --workspace` is the regression oracle.

## Status

- [x] Stage 1 — Code changes (commit `5042965`)
- [x] Stage 2 — Doc changes
- [ ] Stage 3 — Release plumbing
- [ ] Stage 4 — Completeness audit

## Log

### Stage 1 — Code changes

**Implementer**: applied HLD steps 1, 2, 3, 6, 7 cleanly. All five validation
commands pass (modulo two pre-existing environmental issues, both confirmed
unrelated to this PR):

- `cargo build --workspace` fails on `gabby` (missing Vosk lib — documented
  in CLAUDE.md as expected on Windows without `VOSK_LIB_DIR`). Workaround
  `--exclude gabby` passes.
- `cargo test --workspace` is flaky in parallel due to `ice::turn` tests
  toggling shared `static AtomicU64` "force error" flags across threads.
  Pre-existing test design hazard in `crates/rsiprtp/src/ice/turn.rs:120-150`
  (six `force_*_error_once()` functions backed by module-level statics).
  Tests pass with `--test-threads=1`.

**Code reviewer (`code-reviewer` skill)**: APPROVED — no changes requested.
Verified spec adherence per HLD step (1–3, 6, 7), scope discipline (no
out-of-scope edits), correctness (no orphan refs, no broken intra-doc links).

**Devil's advocate**: CLEAR TO COMMIT. Independently confirmed `ice::turn`
flakiness pre-existing (zero `dtls` references in `ice/turn.rs`). Verified
`Cargo.lock` unchanged, no orphan test fixtures, no `.bak` files,
`public-api.txt` carries the dead surface intentionally pending Stage 3
regen. Only DTLS reference left under `crates/rsiprtp/src/` is the
intentional scope sentence at `lib.rs:9`.

**Stage 1 commit**: `5042965` — Remove DTLS-SRTP stub: code changes
(HLD steps 1, 2, 3, 6, 7).

### Stage 2 — Doc changes

**Implementer**: applied HLD steps 4(a/b) and 5(a/b). README install snippet
bumped `0.2` → `0.3` at line 63; WebRTC scope bullet rewritten at line 135.
ARCHITECTURE.md Mermaid label `SRTP & DTLS framing` → `SRTP-SDES` at line
43; module bullet at line 91 trimmed (trailing comma → period). `cargo doc`
clean. Final grep confirms only the intentional `README.md:135` "No
DTLS-SRTP" sentence remains.

**Code reviewer (`code-reviewer` skill)**: APPROVED — no changes requested.
Verified spec adherence, scope discipline, prose flow, Mermaid syntax (the
hyphen in `SRTP-SDES` parallels existing `Sans-IO` at ARCHITECTURE.md:33).

**Devil's advocate**: CLEAR TO COMMIT, with two flags:
- `CLAUDE.md:91` still says `rsiprtp-srtp (SRTP encryption, DTLS)` —
  pre-existing staleness from the workspace collapse (separate HLD), not
  introduced by this PR. **Deferred** to a CLAUDE.md housekeeping pass.
- README advertises `rsiprtp = "0.3"` while `Cargo.toml:15` is still
  `0.2.0` — intentional staging; Stage 3 lands the version bump within
  minutes of Stage 2.

**Stage 2 commit**: forthcoming.
