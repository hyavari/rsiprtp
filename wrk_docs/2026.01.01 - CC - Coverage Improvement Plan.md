# Coverage Improvement Plan

**Date:** 2026.01.01
**Goal:** Increase line coverage from 97.93% to 98.00% (+20 lines).

## Strategy

Focus on the files with the highest number of missing lines to find low-hanging fruit (error paths, edge cases).

## Targeted Files

1.  **`mdsiprtp-transport/src/resolver.rs`** (110 missed lines)
    - **Hypothesis:** DNS resolution error paths, specific DNS record type handling (SRV, NAPTR) edge cases, or fallback logic might be untested.
    - **Action:** Analyze source code for untested branches and add unit tests.

2.  **`mdsiprtp-transport/src/tls.rs`** (74 missed lines)
    - **Hypothesis:** Certificate validation errors, handshake failures, and configuration edge cases.
    - **Action:** Add tests for invalid certificates or connection drops during handshake.

## Execution Plan

1.  **Phase 1: Resolver Coverage**
    - Read `mdsiprtp-transport/src/resolver.rs`.
    - Identify missing coverage areas (look for `Err`, `None`, or complex `match` arms).
    - Implement tests in `mdsiprtp-transport/src/resolver.rs`.
    - Verification: Run `cargo test -p mdsiprtp-transport`.

2.  **Phase 2: TLS Coverage (if needed)**
    - Read `mdsiprtp-transport/src/tls.rs`.
    - Implement tests for error conditions.
    - Verification: Run `cargo test -p mdsiprtp-transport`.

3.  **Phase 3: Final Verification**
    - Run full workspace coverage check.
    - Confirm 98% target reached.
